# PYSPARK-ETL-PIPELINE
Build a complete ETL (Extract-Transform-Load) pipeline using PySpark. Extract from multiple sources (CSV, JSON), transform (clean, join, aggregate), and load to partitioned Parquet — the standard big data pipeline pattern.
